╔═══════════════════════════════════════════════════════════════════════════════╗
║                        ROUTE-SAFE GAP ANALYSIS SUMMARY                       ║
║                      4 Concrete TODOs | 6-9.5 Hours Implementation             ║
╚═══════════════════════════════════════════════════════════════════════════════╝

CURRENT STATE (Post-Hoc Warning Model)
═════════════════════════════════════════════════════════════════════════════════

✗ Coordinator calls generate_evacuation_route(avoid_flood=True)
✗ MapsService routes WITHOUT constraints (avoid_geojson parameter dead code)
✗ Returns safety_rating: "PENDING_ANALYSIS"  ← Contract broken
✗ Analyst tool evaluates route AFTER-THE-FACT via spatial intersection
✗ Routes selected by time/distance, NOT safety

PROBLEM: avoid_geojson never used, avoid_flood parameter ignored, safety_rating
invalid, routes not proactively safe.

───────────────────────────────────────────────────────────────────────────────

SOLUTION (Proactive Avoidance Model)
═════════════════════════════════════════════════════════════════════════════════

✓ Coordinator FETCHES flood extent (Phase 2)
✓ Coordinator extracts avoid_zones from flood polygon
✓ MapsService routes WITH hard constraints via waypoints (Phase 3)
✓ Returns safety_rating: "SAFE"|"CAUTION"|"UNSAFE"  ← Pre-computed
✓ Agent no longer needs post-hoc safety check
✓ Routes guaranteed safe proactively

BENEFIT: Routes safe by default, faster response, safety accurate.

───────────────────────────────────────────────────────────────────────────────

4 CONCRETE TODOS
═════════════════════════════════════════════════════════════════════════════════

TODO 1: Remove Dead avoid_geojson Parameter (PHASE 1 — 30 min)
─────────────────────────────────────────────────────────────────

FILE:    app/services/maps_service.py
LINES:   158-166, 191-195
ACTION:  REMOVE avoid_geojson parameter + parsing logic
         (Replace with avoid_zones in Phase 3)

CHANGE:  get_evacuation_route() signature:
         OLD: avoid_geojson: str | dict | None = None
         NEW: (parameter removed)

SURFACE CODE:
  Line 162: Remove from def signature
  Line 164-166: Remove avoid_geometry = _parse_avoid_geometry(avoid_geojson)
  Line 191-195: Remove route_safety evaluation

BEHAVIOR:
  ✓ Cleaner contract
  ✗ Breaks code passing avoid_geojson (but it did nothing)

TESTS TO UPDATE:
  1. test_maps_service_route_avoidance.py::
     test_selects_safer_route_when_avoid_geojson_provided
     → RENAME to: test_selects_safer_route_when_avoid_zones_provided
     → Use NEW avoid_zones=[{lat, lng, radius_m}] parameter
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_selects_safer_route_when_avoid_zones_provided -v
  
  2. test_maps_service_route_avoidance.py::
     test_raises_for_invalid_avoid_geojson
     → REMOVE (parameter gone, exception no longer relevant)
  
  3. NEW REGRESSION: TestAvoidGeojsonNotDeadCode::
     test_parameter_not_in_signature
     → Assert "avoid_geojson" NOT in signature

EXISTING TESTS VERIFY:
  pytest tests/test_maps_service_route_avoidance.py -v
  BEFORE: 3 passed (with avoid_geojson)
  AFTER:  3 passed (with avoid_zones)

───────────────────────────────────────────────────────────────────────────────

TODO 2: Coordinator Fetches Flood Before Routing (PHASE 2 — 2-3 hours)
────────────────────────────────────────────────────────────────────────

FILE:    app/hawkeye_agent/tools/coordinator.py
LINES:   203-256 + new helper function

CHANGES:
  A. Add flood fetching (after geocoding):
     if avoid_flood:
         from app.hawkeye_agent.tools.analyst import get_flood_extent
         flood_result = get_flood_extent()
         if flood_result.get("geojson"):
             avoid_zones = _extract_avoid_zones_from_flood(flood_result["geojson"])

  B. Pass constraints to routing (line ~239):
     route_result = _get_maps_service().get_evacuation_route(
         origin_latlng=(origin["lat"], origin["lng"]),
         destination_latlng=(destination["lat"], destination["lng"]),
         avoid_zones=avoid_zones,  # ← NEW
         hard_constraint=avoid_flood,  # ← NEW
     )

  C. Return pre-computed rating (line ~254):
     "safety_rating": route_result.get("safety_rating", "SAFE"),  # ← NOT PENDING_ANALYSIS
     "avoid_zones_applied": len(avoid_zones) if avoid_zones else 0,  # ← NEW

  D. Add error case (line ~250):
     if not route_result.get("route_geojson"):
         if avoid_flood and avoid_zones:
             return {
                 "error": "No safe route found avoiding flood zones",
                 "recommendation": "Consider alternative destination",
                 ...
             }

  E. Add helper _extract_avoid_zones_from_flood() function (~50 lines)
     Extract flood polygon as [{"lat", "lng", "radius_m"}, ...]

SURFACE CODE:
  Line 239: Change get_evacuation_route() call
  Line 254: Change safety_rating field
  Line 255: Add avoid_zones_applied field
  NEW: Add _extract_avoid_zones_from_flood() helper after generate_evacuation_route()

BEHAVIOR CHANGE:
  ✓ safety_rating: "PENDING_ANALYSIS" → "SAFE"|"CAUTION"|"UNSAFE"
  ✓ Flood constraints passed proactively
  ✗ BREAKING: Code checking == "PENDING_ANALYSIS" will fail
  ✗ distance_m/duration_minutes may INCREASE (routing around zones)

TESTS TO UPDATE/ADD:
  1. test_route_avoidance.py::TestSafetyRatingPreComputed::
     test_generate_evacuation_route_never_returns_pending_analysis
     → Remove xfail() decorator
     → Assert result["safety_rating"] in ["SAFE", "CAUTION", "UNSAFE"]
     → Run: pytest tests/test_route_avoidance.py::
            TestSafetyRatingPreComputed -v

  2. test_route_avoidance.py::TestCoordinatorFetchesFloodBeforeRouting::
     test_generate_evacuation_route_with_avoid_flood_true
     → Verify mock_flood.assert_called_once()
     → Verify avoid_zones passed to routing
     → Run: pytest tests/test_route_avoidance.py::
            TestCoordinatorFetchesFloodBeforeRouting -v

  3. NEW: TestCoordinatorFloodFailure::
     test_gracefully_handles_flood_fetch_failure
     → Mock get_flood_extent() to raise exception
     → Assert routes without constraints (avoid_zones=None)

EXISTING TESTS VERIFY:
  pytest tests/test_route_avoidance.py -v
  Some xfail until Phase 3 (MapsService accepts avoid_zones)

───────────────────────────────────────────────────────────────────────────────

TODO 3: MapsService Accept avoid_zones Parameter (PHASE 3 — 3-4 hours)
────────────────────────────────────────────────────────────────────────

FILE:    app/services/maps_service.py
LINES:   158-242 (replace) + new methods (~120 lines)

NEW SIGNATURE (replace lines 158-167):
  def get_evacuation_route(
      self,
      origin_latlng: tuple[float, float],
      destination_latlng: tuple[float, float],
      avoid_zones: list[dict] | None = None,  # ← NEW
      cost_surface: str | None = None,  # ← FUTURE (accept, ignore for now)
      hard_constraint: bool = False,  # ← NEW
  ) -> dict[str, Any]:

SURFACE CHANGES:
  Replace entire function body (line ~168-242) with 3-phase algorithm

  Phase A: Waypoint Generation (line ~340)
    if avoid_zones and len(avoid_zones) > 0:
        waypoints = self._compute_waypoints_around_zones(...)
        kwargs["waypoints"] = waypoints

  Phase B: Candidate Scoring (line ~380)
    for each route alternative:
        safety_rating = self._compute_safety_rating_preemptive(coords, avoid_zones)
        candidates.append({..., "safety_rating": safety_rating})

  Phase C: Best Route Selection (line ~420)
    if avoid_zones:
        selected = min(candidates, key=lambda c: (danger_score, duration, distance))

ADD 2 NEW HELPER METHODS (~120 lines):
  1. _compute_waypoints_around_zones(origin, destination, avoid_zones)
     → For each zone on direct path, compute perpendicular offset waypoint
     → Returns [(lat, lng), ...] to route AROUND zones

  2. _compute_safety_rating_preemptive(route_coords, avoid_zones)
     → Count zones route passes near (within radius)
     → Return "SAFE" (0 zones), "CAUTION" (1 zone), "UNSAFE" (2+ zones)

RESPONSE SHAPE (new fields only if avoid_zones passed):
  {
      "route_geojson": Feature,
      "distance_m": int,
      "duration_s": int,
      "safety_rating": "SAFE"|"CAUTION"|"UNSAFE",  # ← NOW PRE-COMPUTED
      "route_safety": {  # ← NEW if avoid_zones
          "avoidance_applied": true,
          "candidate_count": int,
          "selected_candidate_index": int,
          "avoided_zone_count": int,
      },
  }

BEHAVIOR CHANGE:
  ✓ Routes respect avoid_zones (hard constraint via waypoints)
  ✓ safety_rating pre-computed (not PENDING_ANALYSIS)
  ✓ Better selection (safety prioritized when zones present)
  ✗ distance_m/duration_s may INCREASE
  ✗ selected_candidate_index may NOT be 0 (routes reordered)

TESTS TO UPDATE/ADD:
  1. test_maps_service_route_avoidance.py::
     test_selects_safer_route_when_avoid_zones_provided
     → Use avoid_zones=[{lat: 0.0, lng: 0.0, radius_m: 500}]
     → Assert selected route is SAFE_ROUTE (reordered)
     → Assert route_safety.selected_candidate_index == 1 (not 0)
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_selects_safer_route_when_avoid_zones_provided -v

  2. test_maps_service_route_avoidance.py::
     test_preserves_first_route_when_avoid_zones_absent
     → Assert safety_rating == "SAFE" (pre-computed)
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_preserves_first_route_when_avoid_zones_absent -v

  3. NEW: test_hard_constraint_fails_if_no_safe_route
     → Mock API to return []
     → Call with hard_constraint=True
     → Assert route_geojson is None, error present
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_hard_constraint_fails -v

  4. NEW: test_multiple_avoid_zones_generates_waypoints
     → Pass 3 zones on direct path
     → Assert waypoints passed to client.directions()
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_multiple_zones -v

  5. NEW: test_empty_avoid_zones_routes_normally
     → Call with avoid_zones=[] (empty)
     → Assert no waypoints, first route selected
     → Run: pytest tests/test_maps_service_route_avoidance.py::
            test_empty_zones -v

EXISTING TESTS VERIFY:
  pytest tests/test_maps_service_route_avoidance.py -v
  BEFORE: 3 passed (avoid_geojson)
  AFTER:  5 passed (avoid_zones + 2 new)

───────────────────────────────────────────────────────────────────────────────

TODO 4: Update Agent & Analyst Tool Behavior (PHASE 3 — 1-2 hours)
────────────────────────────────────────────────────────────────────

FILE A:  app/hawkeye_agent/agent.py
LINES A: ~107-121 (tool documentation)

FILE B:  app/hawkeye_agent/tools/analyst.py
LINES B: ~814 (_suggest_alternative_route function)

CHANGE A: Agent Instructions
  OLD: "Always call evaluate_route_safety() to check route against flood zones"
  NEW: "Routes are now SAFE by default when avoid_flood=True
        Only call evaluate_route_safety() if user explicitly requests
        Coordinator fetches flood and passes constraints proactively"

SURFACE CODE (agent.py):
  Update documentation block (lines ~107-121) to reflect proactive model

CHANGE B: Analyst Tool (_suggest_alternative_route)
  Extract flood polygon as avoid_zones
  Pass avoid_zones to get_evacuation_route()

SURFACE CODE (analyst.py ~814):
  Add:
    flood_centroid = flood_shape.centroid
    bounds = flood_shape.bounds
    radius_m = max(
        abs(bounds[2] - bounds[0]) * 111_000 / 2,
        abs(bounds[3] - bounds[1]) * 111_000 / 2,
    )
    avoid_zones = [{
        "lat": flood_centroid.y,
        "lng": flood_centroid.x,
        "radius_m": int(radius_m),
    }]

  Replace get_evacuation_route() call:
    OLD: get_evacuation_route(origin_latlng=..., destination_latlng=...)
    NEW: get_evacuation_route(
        origin_latlng=...,
        destination_latlng=...,
        avoid_zones=avoid_zones,  # ← NEW
        hard_constraint=False,  # ← NEW
    )

BEHAVIOR CHANGE:
  ✓ Agent no longer mandates post-hoc safety check
  ✓ Alternative route uses avoid_zones (consistent with coordinator)
  ✓ Faster response (fewer tool calls)

TESTS TO ADD:
  1. NEW: test_route_avoidance.py::
     TestAgentInstructionsReflectProactiveAvoidance
     → Assert "proactive" or "pre-computed" in agent.py docs
     → Assert "PENDING_ANALYSIS" NOT in source
     → Run: pytest test_route_avoidance.py::
            TestAgentInstructionsReflectProactiveAvoidance -v

  2. NEW: test_route_avoidance.py::
     TestSuggestAlternativeRouteUsesAvoidZones
     → Mock _get_maps_service()
     → Call _suggest_alternative_route(...)
     → Assert mock.get_evacuation_route called WITH avoid_zones
     → Run: pytest test_route_avoidance.py::
            TestSuggestAlternativeRouteUsesAvoidZones -v

───────────────────────────────────────────────────────────────────────────────

BACKWARD-COMPATIBILITY CONSTRAINTS
═════════════════════════════════════════════════════════════════════════════════

⚠️  BREAKING CHANGE:
    safety_rating value shift:
      OLD: "PENDING_ANALYSIS"
      NEW: "SAFE"|"CAUTION"|"UNSAFE"
    
    IMPACT: Code checking == "PENDING_ANALYSIS" will fail
    MITIGATION: Update frontend + agent expectations

✓ SAFE ADDITIONS:
  • avoid_zones parameter (optional, default None)
  • avoid_zones_applied field (int, new)
  • route_safety field (object, only if avoidance applied)
  • error case when hard_constraint=True + no route

RESPONSE CONTRACT:
  Without constraints (avoid_zones=None):
    {route_geojson, distance_m, duration_s}  # unchanged

  With constraints (avoid_zones=[...]):
    {route_geojson, distance_m, duration_s, safety_rating, route_safety}

───────────────────────────────────────────────────────────────────────────────

EDGE CASES & FAILURE BEHAVIORS
═════════════════════════════════════════════════════════════════════════════════

CASE 1: No Safe Route (hard_constraint=True)
  Scenario: Flood blocks all paths
  Expected: {"route_geojson": None, "error": "No safe route...", ...}
  Test: test_hard_constraint_fails_if_no_safe_route

CASE 2: Multiple Disjoint Zones
  Scenario: 3+ zones in routing area
  Expected: Waypoints for zones on direct path, others skipped
  Behavior: More complex routing, potentially longer distance
  Test: test_multiple_avoid_zones_generates_waypoints

CASE 3: Flood Zone Contains Origin
  Scenario: Evacuation point in flood zone
  Expected: Error (can't route FROM flooded area)
  Test: test_origin_in_avoid_zone_returns_error (future)

CASE 4: Empty avoid_zones
  Scenario: avoid_zones=[] (empty list after extraction)
  Expected: Routes normally without constraints
  Test: test_empty_avoid_zones_routes_normally

CASE 5: Invalid Flood GeoJSON
  Scenario: _extract_avoid_zones_from_flood() returns [] or errors
  Expected: Log warning, route without constraints (graceful degradation)
  Test: test_invalid_flood_geojson_graceful_degradation

CASE 6: Waypoint Validation Failure
  Scenario: Generated waypoints invalid (water, out of service)
  Expected: If hard_constraint=True, return error; else route unconstrained
  Test: test_invalid_waypoints_fallback_to_unconstrained

───────────────────────────────────────────────────────────────────────────────

COMMIT SEQUENCE
═════════════════════════════════════════════════════════════════════════════════

1. fix: remove dead avoid_geojson parameter from MapsService
   pytest tests/test_maps_service_route_avoidance.py -v
   → 3 passed

2. feat: coordinator fetches flood extent and passes constraints to routing
   pytest tests/test_route_avoidance.py::TestSafetyRatingPreComputed -v

3. feat: implement proactive avoidance in MapsService with avoid_zones
   pytest tests/test_maps_service_route_avoidance.py -v
   → 5 passed

4. docs: update agent instructions to reflect proactive avoidance model
   pytest tests/test_route_avoidance.py -v

5. test: add comprehensive route avoidance test suite
   pytest tests/ -v -k "route_avoidance"
   → 15-17 passed ✓

FINAL VERIFICATION:
   pytest tests/test_maps_service_route_avoidance.py tests/test_route_avoidance.py -v
   Expected: ✓ All pass, no xfail/skip

───────────────────────────────────────────────────────────────────────────────

SUMMARY TABLE
═════════════════════════════════════════════════════════════════════════════════

TODO | Phase | Hours | Risk    | Files                              | Breaking
─────┼───────┼───────┼─────────┼────────────────────────────────────┼──────────
  1  |   1   |  0.5  | LOW     | maps_service.py                    | ✓ param
  2  |   2   | 2-3   | MEDIUM  | coordinator.py                     | ✓ rating
  3  |   3   | 3-4   | MEDIUM  | maps_service.py                    | ✗ param
  4  |   3   | 1-2   | LOW     | agent.py, analyst.py              | ✗ docs
───────────────────────────────────────────────────────────────────────────────
TOTAL   |       6-9.5|         |                                    |

DELIVERABLES LOCATION
═════════════════════════════════════════════════════════════════════════════════

Full Analysis (37KB, 1024 lines):
  /Users/tayyabkhan/Downloads/gemini-agent/ROUTE_SAFE_GAP_ANALYSIS.md

Key Files:
  • app/services/maps_service.py (remove avoid_geojson, add avoid_zones)
  • app/hawkeye_agent/tools/coordinator.py (fetch flood, pass constraints)
  • app/hawkeye_agent/tools/analyst.py (use avoid_zones in alternative routing)
  • app/hawkeye_agent/agent.py (update tool documentation)

Test Files:
  • tests/test_maps_service_route_avoidance.py (5 tests)
  • tests/test_route_avoidance.py (12+ tests)

═══════════════════════════════════════════════════════════════════════════════
